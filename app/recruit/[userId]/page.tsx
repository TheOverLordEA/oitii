import JobPostWrapper from "@/components/postAJob/JobPostWrapper";

export default function Page({}: //   params,
{
  //   params: Promise<{ slug: string }>;
}) {
  //   const slug = (await params).slug;
  return (
    <div>
      <JobPostWrapper />
    </div>
  );
}
